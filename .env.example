# Razorpay (get keys from https://dashboard.razorpay.com)
RAZORPAY_KEY_ID=rzp_test_xxxxxxxx
RAZORPAY_KEY_SECRET=your_secret_key

# JWT secret for auth sessions
JWT_SECRET=your-jwt-secret-min-32-chars

# MongoDB Atlas (optional — uses JSON files locally when unset)
MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net/astroknowledge?retryWrites=true&w=majority

# Render Key Value / Redis (alternative to MongoDB — npm run db:provision:redis)
REDIS_URL=rediss://red-xxxxxxxx:6379

# For automated provisioning: npm run db:provision:api
# Create keys at cloud.mongodb.com → Organization → Access Manager → API Keys (Org Owner)
MONGODB_ATLAS_PUBLIC_API_KEY=
MONGODB_ATLAS_PRIVATE_API_KEY=
MONGODB_ATLAS_ORG_ID=6a2bbe01ad6f8544cdc11316
